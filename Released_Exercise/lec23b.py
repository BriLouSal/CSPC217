matrix = [[0] * 3] * 2
print(matrix)

# It would check the digit as if you try it with a string, it would automatically ignore the \n such as
# that


# The way you can remove via adding the words into the list (which would include the iteration which I think so
# and then remove it into a
